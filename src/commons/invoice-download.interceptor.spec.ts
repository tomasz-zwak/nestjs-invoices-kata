import { InvoiceDownloadInterceptor } from './invoice-download.interceptor';

describe('InvoiceDownloadInterceptor', () => {
  it('should be defined', () => {
    expect(new InvoiceDownloadInterceptor()).toBeDefined();
  });
});
